# Optional read-only dashboard sidecar. Start with:
# docker compose -f images/sync/docker-compose.yml --profile dashboard up -d
DASHBOARD_BASE_PATH=/dashboard

# Basic Auth is disabled when both values are empty. Set both to enable it.
DASHBOARD_USERNAME=
DASHBOARD_PASSWORD=
